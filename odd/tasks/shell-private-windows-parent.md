# Shell private report: Windows parent opener

## Goal and authority
Build a native-tested Windows handle-relative, no-reparse **parent directory opener** for the private report path. This does not enable a Windows private report writer. The approved canonical issue is [Gentleman-Programming/gentle-ai#4935](https://github.com/Gentleman-Programming/gentle-ai/issues/4935); base this feature-branch-chain slice on parent draft [PR #4936](https://github.com/Gentleman-Programming/gentle-ai/pull/4936) head `9442ab6f`. Work on a sibling branch to the Darwin slice, not on top of it. The authorized temporary non-main base for a separate canonical draft child PR is `feat/shell-private-report-ci-base-4936` at the same parent #4936 head `9442ab6f`, because fork Issues are disabled. No fork child PR exists. Manual fork CI below is runtime evidence, not the canonical PR issue gate; no merge, preview, Apply, or report write is authorized.

Strict TDD is required (`/home/devel/.gentle-ai/state.json`, `strict_tdd=true`). Budget: at most 400 changed lines for the implementation slice. Keep `internal/privatefile/report_unsupported.go` unchanged: Windows `WriteNewReport` must still return `ErrUnsupported` without creating a destination. Owner-only ACL at the leaf and exclusive creation remain future work, not claims of this slice.

## Tasks
- [x] **Test-first parent opener.** Add failing Windows-specific tests before implementation for held-handle, component-by-component relative traversal from a trusted directory handle, including real directories and refusal of junctions, symlinks, unknown or unsafe components. Open each child relative to the held parent, reject reparses rather than resolving them, and fail closed on ambiguous metadata/errors; do not substitute a path-string precheck or follow a reparse before checking it. Keep the handle chain alive for the traversal and close handles on failure. Use Microsoft [OBJECT_ATTRIBUTES `RootDirectory` and `OBJ_DONT_REPARSE`](https://learn.microsoft.com/en-us/windows/win32/api/ntdef/ns-ntdef-_object_attributes) as design input, **not** runtime evidence. On Linux, record RED then GREEN via `GOOS=windows GOARCH=amd64 go test -c` with disposable `HOME`, `XDG_CONFIG_HOME`, and `GOCACHE`; compilation alone does not prove native behavior. Do not edit `report_unsupported.go`.
- [x] **Native CI and independent proof.** Exact test-presence guard and native Windows runner passed via manual fork `workflow_dispatch` (not a PR issue gate). Tests exercise junction and symlink parents (including adversarial components), real-directory success, refusal without writes, and handle/cleanup behavior. Exact head, command, run, results and independent source verification/native review are recorded below. No merge or private writer enablement.

## Verification evidence
- Strict TDD: added `internal/privatefile/windows_parent_windows_test.go` first. With disposable `HOME`, `XDG_CONFIG_HOME`, `GOCACHE`, `GOPROXY=off`, `GOTOOLCHAIN=local`, `GOMODCACHE=/home/devel/go/pkg/mod`, `GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go test -c -o "$tmp/privatefile.test.exe" ./internal/privatefile` exited 1 (four `undefined: openWindowsPrivateParent` errors). After implementation, the same cross-compile exited 0. Compilation is not native Windows runtime proof.
- `go test -count=1 ./internal/privatefile` on Linux: pass; `go vet ./internal/privatefile` on Linux and with `GOOS=windows GOARCH=amd64 CGO_ENABLED=0`: pass. Both used the disposable Go environment above.
- Native Windows runtime: manual fork [`workflow_dispatch` run 35957743182](https://github.com/decode2/gentle-ai/actions/runs/35957743182) at exact head `9a5a75c74582956ba0c768ee2a978899a7d90306` passed **11/11**. The exact test-presence guard passed for `TestWindowsPrivateParentOpensPhysicalDirectoriesWithoutTouchingLeaf`, `TestWindowsPrivateParentRefusesReparsesAndNonDirectories` (junction, symlink, non-directory), and `TestWindowsPrivateParentRefusesUnsafeComponentsAndClosesOnFailure`; native `go test -v ./internal/privatefile -count=1 -timeout=300s` passed. `TestWriteNewReportUnsupportedDoesNotWrite` also passed. Existing source was independently verified and native reviewed. This manual run does **not** satisfy the separate canonical PR issue gate.
- Scope: the borrowed root handle must already be trusted; this helper opens existing child directories only. No owner-only ACL, exclusive leaf creation, durable containment against hostile ancestor relocation, or Windows report writer is established. `report_unsupported.go` stays unchanged. Native refusal/cleanup tests passed; this is not proof of the deferred leaf or relocation guarantees. Rollback: remove `internal/privatefile/windows_parent_windows.go` and `_test.go`, remove the Windows CI opener step, and revert this tracker section; leave the unsupported report writer intact.

## Verification and rollback
Linux cross-compile RED/GREEN and native Windows runtime proof are recorded above. Canonical draft child PR issue gate remains separate from manual fork CI. Check focused tests, formatting and diff, and confirm Windows report refusal remains intact. Roll back exactly the opener, its tests, its CI guard/step, and this tracker; leave the parent report writer and unrelated Darwin work untouched.
