# Reapply ODD-only retirement on current gentle-ai main

## Objective and problem
Deliver approved issue #4959: retire SDD/OpenSpec from the current tree so ODD is the only development workflow. The original feature branch was based on a divergent history, and a direct PR would carry unrelated mainline changes. Reapply only the feature onto current main without rewriting or modifying the original branch.

## Scope and constraints
- Authorized branch: `feat/remove-sdd-odd-only-main`, based on `f182ea2018a6399f5d1b6557cf36d71a3df0f723`. Original `feat/remove-sdd-odd-only` and commits `a20da5a0c`, `b07776996`, `688ebca26` remain untouched. Do not read or stage the unknown untracked `internal/cli/AGENTS.md` in any existing checkout.
- Retire SDD/OpenSpec-only code, assets, tests, docs, bench journeys and current-tree historical artifacts; preserve ODD, independent RDD, Strict TDD and user-owned installation assets. Retain original AI-5 installed-byte ownership and rollback protections as part of migration safety.
- User authorized local implementation, work-unit commits, push, PR and merge in `Gentleman-Programming/gentle-ai` via authenticated `gh` session, subject to issue, label, size, CI and ordinary policy gates. The original user choice favored a single coherent PR with documented size exception; applying any protected label to a concrete PR requires fresh exact authorization.
- TDD: `strict_tdd: true` on current-main `openspec/config.yaml` (also active in the original feature); runner `go test ./...`. Reused pre-existing RED/GREEN evidence applies only to original candidate. New conflict fixes require observed RED/GREEN where feasible; do not claim old checks verify new bytes.
- Route: delegated direct for multi-file conflict resolution and broad mapping; parent owns Git worktree state, task tracking and remote operations. The ~400 authored-line work-unit heuristic is advisory. A large coherent removal must not be artificially split or minified.
- Recovery locator: this document and Engram topic `odd/remove-sdd-odd-only-main/tasks` in project `gentle-ai`. Mirror the full current document and its repository-relative path after every task transition.

## Tasks
- [x] **GO-1 — reapply the coherent retirement work unit.** (GO-1a, GO-1b, GO-1c and GO-2a done; rebased work-unit commit `e219644b2`.) Bring original commit `a20da5a0c` onto the new branch without unrelated history; resolve textual conflicts while preserving current-main changes and original branch. Acceptance: no unresolved conflicts, independent hook/model-picker behavior retained, focused checks pass, and one committed coherent work unit recorded. Route: parent mechanical Git application, delegated writer for nontrivial resolution; 13 observed conflicts were settled. Done — integrated behavior and the rebased commit verified with focused checks; full suite remains GO-2/PR CI.
- [x] **GO-1a — preserve main-only ODD routing.** Remove still-active optional SDD routing from `agentguidance`, its tests, and the quickstart while retaining RDD/TDD and the current-main organic policy. Acceptance: installed guidance offers ODD only; focused routing tests RED/GREEN. Route: delegated direct, 2+ nontrivial files. Done — observed evidence exists.
- [x] **GO-1b — preserve current-main model choices without SDD.** Retire SDD roles in Claude, Codex, Kiro and reachable generic OpenCode pickers and SDD-only Codex profiles while retaining real ODD/RDD/native assignments and legacy saved-key compatibility. Restore user-selected Codex ODD model/effort values in the retained owned routing prompt (the original renderer was deleted with the SDD profile); test full LowCost/Powerful model-and-effort matrices and explicit overrides. Acceptance: focused picker/model/install/sync/rendered-prompt tests pass without dropping non-SDD regression coverage. Route: delegated direct, multi-file. Done — observed evidence exists.
- [x] **GO-1c — retire active OpenCode SDD plugin.** Remove SDD Engram Manager from installation catalog, picker and update registration, but retain targeted legacy `DefinitionFor` and uninstall discovery for existing installations. Acceptance: no active offer/update, safe legacy uninstall, and focused regressions pass. Route: delegated direct, multi-file. Done — observed evidence exists.
- [ ] **GO-2 — verify current-main integration.** Run check-only format, focused Go tests for conflicts and ownership, full offline Go suite, vet, bench declarations and offline organic-runtime checks; audit packaged assets, current-tree historical removals, generated ledgers and residual references. Record failures/skips precisely and avoid network or unauthorized live-agent tests. Route: delegated fresh verifier per bounded action. Full offline root suite delegated to PR CI by user decision (local rerun on final bytes cancelled). Online deadcode ratchet now passes; GO-2 remains open for the parent integration and delivery checks.
- [x] **GO-2a — retire SDD deadcode while preserving migrations.** Preserved user-owned OpenCode `default_agent` on sync, retired install-side assignment and restored legacy owned-record uninstall rollback. Retired the SDD-only picker binding, test-only clear helper and orphan row wrapper without changing ODD picker rows. The independent production provider-contract generator retains its review execution API; the orchestrator approved exactly three bundle-only CLI-ratchet baseline exceptions. Acceptance: online ratchet exit 0, focused bundle/reviewassets/picker tests pass; prior behavior changes have their separately recorded RED/GREEN evidence. Route: delegated writer (multi-file). No commit yet.

- [ ] **GO-3 — review and delivery.** Native RDD review declined by user for this candidate; no receipt; delivery proceeds under ordinary repository policy with PR CI as full-suite proof. Prepare a scoped PR linked to approved #4959 and apply exactly one `type:*` label and protected size exception only under exact target-host authorizations. Merge only after required checks pass. Route: parent for Git/authority/policy, bounded workers for verification.

## Evidence and current state
- New isolated worktree created from exact GitHub main `f182ea2018a6399f5d1b6557cf36d71a3df0f723`, with tracker commit `db00c3b61`. Original branch remains at `688ebca26`; Gentle Shell PR #1408 merged separately.
- Applied `a20da5a0c` without committing; 13 conflicts (seven content, six SDD-only modify/delete) resolved. Ported main's independent Claude hook test to retained `agenthooks` and corrected Windows PowerShell command/legacy-hook pruning after exact user scope authorization. Native Windows execution is unavailable on macOS.
- Codex ODD prompt binding restored in the owned Codex routing block via `agentRoutingGuidanceStep` (`internal/cli/run.go`) → `agentguidance.InjectRoutingWithOptions`. `TestCodexODDAssignmentInstallRoutingStep`, `TestCodexODDAssignmentSyncPropagatesSavedSelections`, `TestCodexODDVerificationDoesNotRequireRetiredProfiles` passed, plus the preset matrix test `TestCodexODDPresetCarrilModelAndEffortMatrix`.
- Kimi: 10 orphan `sdd-*` subagents and `sdd-orchestrator` include removed (`internal/assets/kimi/agents/gentleman.yaml`, `KIMI.md`); Kimi raw reviewer templates `review-{risk,readability,reliability,resilience}.md` stripped of obsolete SDD/OpenSpec ledger body (installer renders canonical `ReviewerPrompt`; frontmatter retained); `gentleman.yaml` installed-hash fixture updated in `internal/components/reviewassets/install_test.go`; `go test ./internal/assets ./internal/components/reviewassets` passed.
- The `work-unit-commits` skill (embedded `internal/assets/skills/work-unit-commits/SKILL.md` and root `skills/work-unit-commits/SKILL.md`) now has an ODD Relationship section instead of an SDD Relationship section.
- Stale references to the deleted `internal/sddstatus` package removed: `.github/workflows/ci.yml` Windows package list, `scripts/darwin-release-blockers.manifest` (5 `TestEditGrant*` rows; observed RED verify exit 1 → GREEN `manifest verified: 39 entries across 2 packages`), PR template, Hermes skill link, organic guide Flow 22 marked retired/historical.
- e2e organic runtime Cursor test migrated off "Optional SDD" (RED at `organic_runtime_test.go:1914` → 5 targeted tests GREEN).
- Full offline root suite (prior 857-path candidate) completed exit 1: 77 ok, 4 no-test, `internal/cli` FAIL — `TestEveryRegisteredGuardPopulationDeclarationMatchesProduction` and `TestEveryProductionRefusalNamesResolutionOrDeclaresByDesign` opened the deleted `../sddstatus` path (fixed by removing that scan root in `internal/cli/guard_population_test.go` and `refusal_resolution_ratchet_test.go`; both GREEN; guard count 5 unchanged; ratchet baseline had 0 `sddstatus` entries; field-carried refusals 30 ≤ ceiling 70), plus a package-level 600s timeout during `TestReviewRecoverSelfDerivationExplicitWrongBindingStillRefuses`, which passes alone in 1.70s (aggregate budget, not a proven hang). Local rerun on final bytes was cancelled by user decision; full-suite proof is delegated to PR CI.
- Other checks: `go vet -p 2 ./...` exit 0 (857-path candidate); nested `bench` `go test ./...` exit 0 (declarations only, no driven journeys); `python3 -B -m unittest discover -s scripts -p test_opencode_v2_host_test.py` 11 OK.
- Earlier staged snapshot: 860 staged paths, 11736 insertions, 118023 deletions; `git diff --cached --check` and gofmtcheck passed then. Subsequent authorized unstaged work changed the candidate; these historical checks do not certify the final bytes.
- Initial online deadcode ratchet (deadcode@v0.30.0): exit 1, 33 new unreachable functions and 7 stale baseline entries; GO-2a subsequently resolved these (final result below). Offline ratchet cannot run with `GOPROXY=off` because the x/tools deprecation lookup is blocked.
- Not available/unauthorized: native Windows run, external-agent journeys, driven bench, v3.7 packed download. Native RDD review declined by user for this candidate; no receipt; delivery under ordinary repository policy with PR CI as full-suite proof.
- **GO-2a earlier continuation (historical evidence; blockers resolved):**
  - `screens.ClearModelPickerAssignment`: base's sole production caller was the retired `ScreenProfileCreate` SDD profile editor. An unjustified attempt to add backspace clearing to the independent ODD picker was reverted. The helper and its four now-orphan tests were subsequently removed under expanded edit authority; ODD picker tests remain.
  - `uninstall.isRetainedNativeAgent` — new orphan helper, deleted. No code path in `uninstall/service.go` enumerates or deletes the sub-agents directory at all anymore (the whole `case model.ComponentSDD:` block that did this at base was removed with the component); native review/Judgment Day agent files are installed unconditionally by `reviewassets.InstallNativeAgents` and are never touched by any uninstall path today, so the helper's retention purpose is already covered by the absence of any deletion code. Replaced the pure-unit test with `TestCompleteUninstallPreservesNativeReviewAndJudgmentDayAgents` (`internal/components/uninstall/service_test.go`), which installs real native agent files and runs `CompleteUninstall()` to prove they survive.
  - SDD-only leftovers deleted with their tests (confirmed zero non-test callers): `assets.SDDCommandsAssetDir` (file removed), `model.ModelCapability` (file + test removed), `filemerge.RemoveTopLevelJSONCValue`/`topLevelJSONCPropertyRange`, `reviewtransaction.PublishFileNoReplace`/`ReplaceFileAtomic`, `tui.hasSelectedAgent`.
  - Verified-individually siblings resolved: `agentguidance.InjectRouting`/`InjectStrictTDD` wrappers deleted, ~29 test call sites repointed to `InjectRoutingWithOptions`/`InjectStrictTDDWithOptions` (mechanical, string literals in `t.Fatalf` messages left untouched); `reviewassets.OpenCodeReviewerPrompt` deleted (OpenCode isn't in `NativeAgentManifest`, its review-provider roles are hardcoded separately in `run.go`, so this wrapper had no delivery path at all, not even at base's completed branch); `model.RenderCodexPhaseEffortsByPhase`/`phaseToCarrilModel`/`codexPhaseOrder` deleted (the legacy per-phase `sdd-orchestrator.md` renderer, fully superseded by the restored `RenderCodexODDAssignments`); `model.CodexODDRoleCarrils` kept and wired as the live iteration source inside `RenderCodexODDAssignments` (was previously a test-only accessor over the same `codexODDRoles` data the renderer already used internally).
  - `opencodedefault`: user decided sync must never override `default_agent`, consistent with the two existing CLI sync tests. Removed only the SDD-era `PrepareInstall`/`InstallPlan.Apply` and `newOwnership`, retaining ownership-record parsing and uninstall rollback. `uninstall.Service.buildPlan` now snapshots both settings and ownership metadata on full OpenCode removal and composes legacy rollback with other settings rewrites. `UninstallPlan.Apply` rolls back only when a valid record exists and the current default is still the managed value; without a record, or after a user change, it leaves the default alone and retains user-modified metadata. Writes preserve existing file permissions. RED: `TestCompleteUninstallLegacyOpenCodeDefaultOwnership/owned_default_rolls_back` failed with `gentle-orchestrator` still set; GREEN: both owned and user-modified cases pass. Unit cases cover absent prior value, malformed metadata, unowned managed default, and permissions. This does not reinstate install-side assignment.
  - Earlier ratchet exited 1 on six moved `reviewassets` symbols, `modelPickerRowsWithCustom`, and the test-only `ClearModelPickerAssignment`. The test-only clear helper and its four tests were removed before this continuation. Current ratchet results and bundle migration evidence follow below.
  - Verification on continuation bytes: offline `go build ./...` and `go vet -p 2 ./...` pass; focused package group including `reviewtransaction` passes (248s); CLI focused command fails only `TestComponentSyncStepCodexRuntimeGate` (expects GPT-6 SDD profile writes; causation relative to base was not proven and it is outside authorized edits). `go run ./internal/gofmtcheck` fails because the index still tracks deleted working-tree files (`internal/assets/commands.go`, `internal/model/capability.go`, `internal/model/capability_test.go`, `internal/model/codex_model_internal_test.go`); no staging was authorized. One live formatting issue in `ownership_test.go` was fixed after that command. `git diff --check` passes.
  - Online ratchet at the end of that earlier continuation reported eight entries; later changes resolved them.
- **GO-2a final continuation:** Base `f182ea2018a6399f5d1b6557cf36d71a3df0f723` used `sdd.ReviewExecutionContractFor` in `internal/providercontractbundle/bundle.go` to generate the independent Pi RDD contract. The current bundle calls `reviewassets.ReviewExecutionContractFor` through `internal/providercontractbundle/review_execution.go`; `go run ./internal/providercontractbundlecmd generate --out ...` is an independent production entrypoint, also invoked by release policy (`internal/releasepolicy/policy.go:556,734`). The deadcode ratchet scans only `./cmd/gentle-ai`, so the orchestrator explicitly approved baselining the three symbols reachable from the bundle's separate entrypoint: `reviewassets.ContractFor`, `reviewassets.ReviewExecutionContractFor`, `reviewassets.selectTransport`. No lost RDD wiring was inferred. Unused `Contract`, `SelectCaptureTransport`, `BindIdentity` and orphan `modelPickerRowsWithCustom` wrappers were removed in the prior continuation; independent reviewer installation and ODD picker tests remain intact. `TMPDIR=/private/var/tmp GOMAXPROCS=2 ./scripts/deadcode-ratchet.sh --update` changed `.deadcode-baseline.txt` by exactly those three additions and seven stale `sdd/*`/`sddstatus` deletions, with no other baseline changes. The online ratchet rerun printed `no new unreachable functions` (exit 0). Offline `go test ./internal/providercontractbundle ./internal/components/reviewassets ./internal/tui/screens -count=1 -timeout 600s -parallel 2 -p 2` passed (all three packages); the prior offline `go build ./...`, `go vet -p 2 ./...`, and `git diff --check` passed before this baseline/tracker-only update. A bundle generate/verify smoke test was not run: its required generated output would be outside the authorized edit surfaces. No files staged, no receipt, no RDD invocation.

- Rebased on `origin/main` `40b35ee21`: tracker `a412f30cd`, work-unit `e219644b2`. The only conflicts were `bench/review_declarations.go` and `bench/testdata/journeys.manifest`; preserved main's `j4433-restored-escalated-derived-target-stops` while removing SDD-only `j44`/`j128`. Post-rebase `go run ./internal/gofmtcheck` exit 0, nested `bench` `go test ./... -count=1 -parallel 2 -p 2` pass, online deadcode exit 0 (`no new unreachable functions`), and `git diff --check origin/main...HEAD` clean. Focused CLI/packages, vet and gofmt passed immediately before rebase; the final full root suite and Windows remain PR CI checks.
- Final staged-candidate verification before rebase: focused CLI `TestComponentSyncStepCodexRuntimeGate|Sync|Uninstall|OpenCode|Codex|DefaultAgent` pass (87.800s); grouped packages opencodedefault, uninstall, agentguidance, reviewassets, providercontractbundle, tui, assets, model, filemerge, reviewtransaction pass (reviewtransaction 350.578s); `go vet -p 2 ./...`, gofmtcheck, cached diff check, and online deadcode all exit 0. No local full suite by user choice; no RDD receipt.

## PR #4967 CI follow-up (GO-2 remains open)
- CI run `36072321752`: Unit Tests job `107875936944` failed the portable driven journey `j127-customizable-install-rdd-choice` after the installer introduced a Strict TDD Mode screen between preset and Community Tools. The PTY timed out waiting for Community Tools while displaying STRICT TDD MODE. Journey now advances that intervening screen, retaining the RDD ON → OFF → cancel assertions. Parent independently built the bench harness and product and ran the focused `gentle-ai-bench run --binary ... --only j127-customizable-install-rdd-choice --out ...` against the current bench diff: exit 0, 1 completed, 0 unsupported, 0 failed. This is parent-reported driven GREEN; the exact binary/output paths were not supplied to this worker.
- Ubuntu/Fedora/Arch E2E jobs `107875937082`/`107875936984`/`107875936967` each reported 39 failed and 271 passed. Ubuntu's log confirms unsupported `sdd` component and removed `--sdd-mode` flag under old installer assertions. E2E script now removes retired SDD-only function bodies, flags and assertions while retaining preset, foundation-skill, memory, persona, ODD routing, permissions, theme, idempotency, GGA, backup and user-owned configuration checks. The retired OpenCode command-corpus test was removed because its sole installer path required the removed SDD component; independent OpenCode asset tests remain elsewhere. Independent verifier caught one remaining Tier-2 idempotency test that still used minimal preset while requiring nonempty skills; changed only that test to ecosystem-only, preserving its byte-idempotency assertion. Restored the independent Windsurf persona+Engram coexistence checks that had been bundled with a retired SDD test, and added an ODD routing assertion; no SDD-only function remains. Docker daemon was unavailable locally; full three-lane E2E proof awaits CI. Local Tier-1 dry-run with isolated HOME and built CLI passed (66 passed, 0 failed, 3 platform skips); GO-2 is not complete until the new CI run passes.
- Parent is authorized to stage, commit and push under ordinary repository policy, and to merge conditionally only after all checks pass. The worker must not stage, commit, push, merge, or invoke native RDD. Review was declined for this candidate; no receipt.

## PR #4967 second CI follow-up (GO-2 still open)
- Run `36074761643` on HEAD `15025cf86` failed Unit Tests while compiling the bench fixture: `internal/app/bench_model_picker_fixture.go:98` referenced the retired `ModelPickerState.ForProfile` field. The sync-screen diagnostic now prints the current ODD picker's `SelectedPhaseIdx` instead; cursor, row count, custom agents, catalog IDs, model-config state and picker mode remain in the error. Local `go build -tags bench_fixture ./internal/app ./cmd/gentle-ai` passed and `go test -tags bench_fixture ./internal/app -count=1` passed (34.000s). This proves fixture compilation locally, not the CI journey.
- Ubuntu/Fedora/Arch E2E each reported the same four failed assertions (at old script lines 550, 766, 953, 1027). `internal/components/skills/presets.go` retains `judgment-day` as the sole minimal skill, and the CI installations counted one skill in both Claude and OpenCode. Both assertions now require exactly one `SKILL.md` **and** the `judgment-day/SKILL.md` path. OpenCode ODD routing is installed in the owned `gentle-orchestrator` prompt inside `opencode.json` rather than `AGENTS.md`; both assertions now check the settings file's routing marker while keeping the independent AGENTS.md persona/memory checks. Source `agentguidance/inject.go` and existing CLI installation tests verify that prompt carrier.
- Local `bash -n e2e/e2e_test.sh`, `go run ./internal/gofmtcheck`, `git diff --check`, `go test ./internal/components/skills -run '^TestSkillsForPresetMinimalRetainsJudgmentDay$' -count=1`, and `go test ./internal/cli -run '^TestInstall(DeliversRoutingGuidanceWithoutSDDComponent|RoutingGuidanceSurvivesOpenCodePluginInstall|RoutingGuidanceWorkspaceScopeDeliversOpenCodeToHome)$' -count=1` passed. The E2E script's install tier calls `cleanup_test_env` (`rm -rf` on HOME configuration), and isolated HOME execution would write outside this worker's exact allowed edit surfaces, so local Tier 1/Tier 2 script execution was not attempted here. Linux full E2E and Unit Tests rerun remain CI proof; no CI green is claimed.
- Rollback boundary: only the bench fixture diagnostic, four E2E expectations and this GO-2 evidence entry. Candidate intentionally unreviewed; no native RDD invocation, staging, commit, push or merge from this worker.

## PR #4967 third CI follow-up (GO-2 remains open)
- Run `36076234123` failed only Unit Tests at model-picker axis `j97-opencode-custom-agent-model-picker-runtime`: `persisted model = "", want "bench-provider/bench-model"`; all E2E Ubuntu/Fedora/Arch and Darwin/Windows passed on this head. Parent reproduced j97 locally with the tagged fixture (one failed). This is not a stale journey assertion: the TUI selected and rendered the custom assignment, but OpenCode sync's retired `ComponentSDD` is a no-op and no retained step persisted `ModelAssignments` into `opencode.json`.
- Restored an OpenCode-only model assignment step on the ordinary sync route after routing guidance, with the settings path included in sync backup targets and changed-file reporting. It merges current picker identities and existing custom agents only, without reviving legacy SDD keys. An assignment without effort omits `variant` to preserve the user's custom-agent value (#3262); explicit effort updates it. RED: offline `go test ./internal/cli -run '^TestSyncOpenCodeCustomAgentAssignmentPreservesUserVariant$' -count=1 -timeout 180s -parallel 2 -p 2` failed with `persisted custom model = <nil>`. GREEN: the same focused test passed after wiring; negative tests for retired SDD keys, non-OpenCode sync, explicit effort, changed paths and idempotence passed in a focused two-test run. The broader offline CLI `-run 'Sync|ModelPicker|OpenCode|CustomAgent'` passed (105.403s); nested bench declarations passed, but do not prove driven execution.
- The offline app/tui/opencodedefault suite had only two app integration failures: child `go build` commands attempted uncached module downloads with `GOPROXY=off`; TUI/screens/opencodedefault packages passed. At this point, driven j97 GREEN had not yet been observed; parent subsequently ran the hardened tagged fixture successfully (see follow-up below). The worker did not build into `/private/var/tmp` because generated writes outside the authorized edit surfaces are prohibited. No native RDD, staging, commit, push or merge.
- Rollback boundary: the ordinary OpenCode sync assignment step and its backup target in `internal/cli/sync.go`, behavioral cases in `internal/cli/sync_test.go`, and this GO-2 evidence entry.

## PR #4967 third CI follow-up: symlink safety correction (GO-2 remains open)
- Independent verification found that the initial assignment step used `ReadFile`/`Stat`/`WriteFile` through a leaf symlink to a target outside the sync backup scope. RED: `GOPROXY=off GOSUMDB=off GOTOOLCHAIN=local go test ./internal/cli -run '^TestSyncOpenCodeAssignmentRejects(SettingsSymlink|NonRegularSettings)$' -count=1 -timeout 180s -parallel 2 -p 2` failed both symlink cases (`sync accepted leaf symlink`), including targets inside and outside HOME; the non-regular directory case already refused.
- The step now uses `Lstat` to refuse any non-regular leaf before discovery/read, and `filemerge.WriteFileAtomic` to publish without following a leaf symlink at the write boundary. The writer preserves existing mode and reports a landed replacement even if later durability fails, so sync still reports the changed path for rollback. GREEN: all focused symlink/non-regular/custom variant/legacy-identity tests passed after the fix; the full offline CLI `-run 'SyncOpenCode|Sync|ModelPicker|OpenCode|CustomAgent'` passed (80.425s). The direct-step regression proves assignment-step refusal for inside/outside-HOME symlink targets and non-regular settings paths. A separate full-plan regression primes guidance, swaps the settings leaf for a symlink, and asserts that `sync:agent-guidance:opencode` refuses first, before the assignment step runs, without modifying the target or replacing the link. No SDD workflow was reactivated.
- Parent ran the hardened tagged fixture and driven j97 after the fix: `1 completed, 0 failed`, output `/private/var/tmp/gentle-ai-4967-bench/j97-hardened-green.json`. Parent spot-checked the five focused CLI cases, gofmtcheck and diffcheck; all passed. Work-unit commit `a92d4f9c9f78b5bb65e2169cf128c75891eca92f` contains the sync fix and regression tests. CI on committed bytes remains required before GO-2 closes. No native RDD invocation or merge.

## PR #4967 fourth CI follow-up (GO-2 remains open)
- Pushed head `72ce8aa02`, CI run `36078688990` Unit Tests job `107895616813`: refusal-ratchet rejected the new non-regular OpenCode settings error because it named no resolution. Three sync tests failed under CI's `XDG_CONFIG_HOME="$HOME/config"`: they hardcoded `~/.config/opencode/opencode.json` while the production path honors XDG. Other platform results from this run are not inferred here.
- RED locally: with per-test `XDG_CONFIG_HOME` set to an independent `t.TempDir()`, focused `TestSyncOpenCode` tests failed for missing settings, missing persisted model and unchanged hardcoded path; `TestEveryProductionRefusalNamesResolutionOrDeclaresByDesign` rejected `internal/cli/sync.go:734`. The tests now locate settings using `effectiveOpenCodeSettingsPath` and an OpenCode adapter, preserving the exact persistence/symlink assertions under arbitrary XDG. The non-regular-path regression now requires a regular-file resolution and a rerunnable sync command. The production refusal tells users to move a symlink or directory aside, place a regular settings file, then rerun `gentle-ai sync`; no ratchet baseline change.
- GREEN: offline `go test ./internal/cli -run 'TestSyncOpenCode|TestEveryProductionRefusalNamesResolutionOrDeclaresByDesign' -count=1 -timeout 600s -parallel 2 -p 2` passed (0.399s), with CI-like XDG supplied by the tests' isolated temporary directories. Parent spot-check with external CI-like `XDG_CONFIG_HOME`, gofmtcheck and diffcheck also passed. Work-unit commit `3021ad4b08ba57f92fb36f22db533c76aeffaf52` contains these production/test fixes. Attempted full `go test ./internal/cli -count=1 -timeout 600s -parallel 2 -p 2` timed out at 600s during `TestNegotiatedStatusWithUnknownLineageFailsClosedFromForeignRepository`; it is not proof of a new production failure or a passing full suite. The broad run exercised review tests and emitted noninteractive review messages, so no further broad local runs are planned. GO-2 remains open until CI on final committed bytes passes. No native review explicitly invoked and no merge.

## Next step
Parent should review this scoped diff and rerun PR CI Unit Tests on committed bytes. Local driven j97 is parent-reported GREEN; GO-2 remains open until required CI checks pass. This candidate remains UNREVIEWED by explicit decline; no native RDD receipt.
