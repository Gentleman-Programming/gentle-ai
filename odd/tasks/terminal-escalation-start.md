# Terminal escalation START collision (#4433)

## Objective
Prevent selectorless STATUS or INSPECT from advertising a fresh START whose derived lineage is already terminal `escalated` for the same candidate, while preserving a safe provider-owned path for a genuinely new candidate.

## Problem and evidence
Issue #4433 documents `escalated` closure followed by `fresh_target_ready` with the same lineage and target; executing that START fails `atomic_start_conflict` on immutable `state`. Abandon refuses terminal authority. The original report and nine comments include intended-untracked scope loss and variants on multiple versions/platforms. The exact selectorless STATUS → consent-granted START conflict was reproduced on clean current main by the T1 regression. Clean current base: `origin/main` at `f182ea2018a6399f5d1b6557cf36d71a3df0f723` (2026-09-23); the existing checkout and other worktrees are out of scope. Issue label `status:approved` verified.

## Authorized scope and constraints
Local changes in this isolated worktree only. No remote writes, manual authority repair, release, or PR; native review runs only under the enabled user-owned switch and its consent. Preserve immutable terminal safety and do not force new lineages solely to evade an escalated result. Related PR #4941 changes inconclusive closure into `decision_required`; #4899 handles historical store exits; the approved-acknowledgement terminal-consumption branch is not escalation. Check for conflicts before editing. Source-mutating normalization precedes candidate review.

## TDD and delivery
Strict TDD is active by explicit user choice (`1`) after the bench worker requested mode resolution. Observe RED before each behavior-changing fix, GREEN afterward, then refactor/check; runner: focused `go test -count=1 ./internal/cli -run '<test name>'` for CLI changes, `go test ./...` in `bench/` plus the CI-derived focused driven bench command for the bench journey. Test presence alone did not enable this mode. Delivery strategy: ask-on-risk. Forecast: ~250–400 authored changed lines plus bench if required; reassess before passing 400, no code golf. Engram mirror `odd/terminal-escalation-start/tasks` is pending because the current Pi session is bound to `gentle-pi`, not `gentle-ai`; do not save this document under the wrong project.

## Tasks
- [x] T1 (delegated): Reproduce the exact selectorless STATUS/START collision on current clean main with a behavior-first regression and negative controls; capture RED command and outcome. Allowed edit surfaces: `internal/cli/*_test.go`, `internal/reviewtransaction/*_test.go`, `bench/journeys*.go` only as needed. Acceptance: actual emitted transition tested, not a proxy assumption; clean-main failure observed. Checks: focused `go test`.
- [x] T2 (delegated): Correct the governing authority classification/transition at the root, preserving immutable terminal state, eligible recovery, new-target START, and intended-untracked scope. Allowed edit surfaces: `internal/reviewtransaction/target_status.go`, `internal/cli/review_facade.go`, `internal/cli/review_next_transition.go`, associated tests and provider contract docs only if behavior changes. Acceptance: T1 green and no impossible continuation advertised. Checks: focused Go tests and any applicable driven bench journey.
- [x] T3 (delegated verification): Normalize and run applicable full impacted package suites, build/vet/format, driven journey and independent read-only review on exact candidate. Record failed, unavailable, skipped checks honestly. Both impacted packages passed under isolated HOME and canonical TMPDIR; the committed source work unit received its own approved and acknowledged native review.

## Progress
- T1 RED observed on current `origin/main` in `internal/cli/review_next_transition_terminal_test.go`: selectorless STATUS advertises `fresh_target_ready` for an escalated occupied lineage; its emitted START and exact granted invocation fail `atomic_start_conflict` with `mutation_outcome: not_started`. Command: `go test -count=1 ./internal/cli -run '^TestSelectorlessStatusDoesNotRestartEscalatedCandidate$'` (expected FAIL at line 155 before production edit). The same test now passes.
- T2 initially implemented in `internal/reviewtransaction/target_status.go` and `internal/cli/review_facade.go` with a core restored-original regression. Focused tests passed, but T2 was reopened when independent verification exposed three unborn-repository STATUS regressions from the facade's unconditional second assessment. The exact derived-lineage occupancy guard was implemented with observed strict TDD RED→GREEN on all three failing unborn controls plus #4433; gofmt/diff checks passed. Independent readback also found a false-green early return in the CLI regression, now tightened and focused-green. Worktree `/Users/alanbuscaglia/work/gentle-ai-worktrees/issue-4433-current`, branch `fix/4433-terminal-start-conflict-current`, clean at creation. Earlier worktree `issue-4433` at stale local `main` was left intact after forced-update of `origin/main` prevented fast-forward; do not base this fix on it.
- Strict TDD follow-up: the three unborn-repository STATUS tests failed RED on the candidate's unconditional core assessment, then passed GREEN with the exact derived-lineage occupancy guard; the #4433 regression remained green. The bench corpus first failed RED without declaration/manifest, then passed GREEN; the final check-only formatter and `git diff --check` passed.
- Final source/test diff: 263 authored additions plus deletions across seven tracked files, plus this feature document. `go build ./...` and `go vet ./...` passed. Focused CLI/core terminal, recovery, and unborn controls passed. `cd bench && go test ./...` passed. CI-derived built-binary driven journeys `j4433-restored-escalated-derived-target-stops` and unchanged `j123-rejected-provider-validator-starts-fresh-high-risk-review` each completed (1 completed, 0 unsupported, 0 failed); j4433 recorded one expected terminal-stop block.
- Full impacted `go test -timeout 40m -count=1 ./internal/reviewtransaction/... ./internal/cli/...` failed with 30 top-level tests (18 reviewtransaction, 12 cli), mostly lock/path and install/sync failures. No unborn STATUS regression remained. A bounded sample of four named failures was reproduced on pristine detached `origin/main` at `f182ea201`; this confirms only those four baseline failures, not all 30. The initial default-timeout run also failed at 10 minutes. At that point, the impacted suite was not passing. No full-tree `go test ./...` was run. The baseline install test warned of a possible global agent-config side effect; no before/after global-config evidence or cleanup is claimed.
- Isolated full-suite proof: `env -u PI_CODING_AGENT_DIR HOME=/private/tmp/gentle-ai-isolation-6vE1t2 GOMODCACHE=/Users/alanbuscaglia/go/pkg/mod GOCACHE=/Users/alanbuscaglia/Library/Caches/go-build GOPATH=/Users/alanbuscaglia/go TMPDIR=/private/tmp GOPROXY=off go test -timeout 40m -count=1 ./internal/reviewtransaction/... ./internal/cli/...` exited 0 after 687 seconds; both packages passed. The prior default-environment failures remain recorded above and are not silently erased. Whether the temporary HOME changed could not be established because the metadata-check `stat` format was unsupported; actual user-home access was not traced. No full-tree `go test ./...` run.
- Work-unit commit `c2ed12a58` (`fix(review): stop restarting escalated original targets`) contains the seven source/test/bench paths. Its committed range against `f182ea201` received native 4-lens approval and acknowledgement: lineage `review-e6a6a003868efa74`, target `sha256:3ec2b1c10eac81dccd46277e58d14094d5fa51d448c0134cb5c2aff9839cc737`. An earlier workspace review of an eight-path snapshot (`review-fffe2951f09b05a2`) was also approved and acknowledged, but that snapshot preceded this evidence update and is not claimed as the final commit review. Review results do not authorize delivery.
- This task document was committed as a separate passive evidence work unit. Native committed-range assessment against `c2ed12a58d69f01085bd461c1e423c0c353fc129` returned `risk: passive`, `review_due: false`, `review_due_reason: passive` for this Markdown-only change; structural readback passed. Engram mirror pending due to session project binding; no push or PR.

## Next step
Report the local outcome plus the skipped full-tree suite and pending Engram mirror. Push or open a PR only with separate user authorization.
