STRICT TDD MODE

Should agents follow Strict TDD (RED → GREEN → REFACTOR) for every task?
When enabled, agents write tests first and confirm failure,
then implement the minimum code to pass before refactoring.

▸ ( ) Enable
  (*) Disable

  Back

j/k: navigate • enter: select • esc: back