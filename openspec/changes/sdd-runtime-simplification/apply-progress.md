# Apply progress: AI unit 1

## AI unit 2 preflight — execution admission clarification pending

Parent reports AI unit 1 independently PASS and its native objective COMPLETE, with evidence
`/tmp/sdd4484-ai1-final-verify.HFimty/` and manifest
`82f0a69757ba84f790edbce689f5afb1d7767a1a1b1452457313313a72665647`.
Older pending notes below are historical; this writer did not independently rerun unit 1.
Consumed `/tmp/sdd4484-ai-status-unit2.1foqeM.json`: apply ready, 4 checked/10 pending,
no blockers, repo-local authorized root; no action-context warning. Delivery remains one
size-exception PR per repository, no chain or delivery action; this unit allowance is 400 A+D.
Execution paused: injected native instructions require an executor acquire, while the parent
explicitly forbids child acquire/settle and retains the new token. Clarification was requested;
no attempt operation, test command, production/test edit, or owned process was launched.
CodeGraph mapped `ForAgent`/`Admit` as native executable admission. Existing research-state tests
read shipped managed contracts through `assets.Read`; recovery, no-interview and grouped-prompt
clauses are guidance characterization, not native execution proof of host behavior.
Task 3.1 remains unchecked and its native and Pi portions remain pending in this unit.
Exact pre-edit bytes/hashes: `/tmp/sdd4484-ai2-preflight-rryicclq/baseline/` and `baseline.json`.
TDD Cycle Evidence: safety net/RED/GREEN/triangulation/refactor not run; no coverage claim.
Rollback: remove only this appended preflight section; source/tests/tasks remain unchanged.
Writer: `PI_PROVIDER=openai-codex`, `PI_MODEL=gpt-6-astra`; effort not exposed.

## AI unit 2 — native preservation implemented, independent verification pending

Parent clarified this is the already-admitted child launch; the preflight pause above is resolved.
No second acquire, token transfer, settle, reset, actor launch, production edit or delivery occurred.
Only the native characterization portion of 3.1 is implemented. Whole 3.1 remains unchecked;
all ten remaining task lines listed below remain unchanged. Pi execution and parent independent
verification remain required; this is not final SDD verification or host-runtime acceptance.

### TDD Cycle Evidence — preservation, not defect RED

Evidence directory: `/tmp/sdd4484-ai2-preflight-rryicclq/`; every executed command has full
`.stdout`, `.stderr`, `.command`, `.pid`, `.supervisor.pid` and real `.exit` records.

| Stage | Command / evidence | Result |
|---|---|---|
| Safety net | `go test ./internal/agents/researchcapability ./internal/components/sdd -count=1 -json`; `baseline.*` | Exit 0; 1,041 test/subtest passes |
| RED | No demonstrated defect; production is read-only | N/A; first-pass cases are coverage-gap preservation, never fabricated RED |
| GREEN / triangulation | Same exact focused command; `focused.*`, `named-test-records.json` | Exit 0; 1,047 test/subtest passes, including all three new names and untouched hybrid restart test |
| Refactor | None needed; existing fixtures reused | No production changes; only ownership comment clarified after focused run |
| Full module | `go test ./... -count=1`; `full.*` | Exit 0; single owned run, bounded monitoring in `monitor.jsonl` |
| Quality | `go vet ./...`; `go run ./internal/gofmtcheck`; `git diff --check`; `vet.*`, `gofmt.*`, `diff-check.*` | All exit 0 |

`contract_test.go` adds executable `ForAgent`/`Admit` coverage for all 26 grant subsets across
five declared agent/class pairs, including empty, incomplete and full sets. Existing tests pin
exact closed declarations, renamed/duplicate/extra grants, unknown classes/schema and denial claims.
`research_state_contract_test.go` adds the two required handoff/grouped-prompt names against
actual shipped lifecycle/proposer assets. These assert SHIPPED INSTRUCTIONS ONLY, not executed
recovery, no-interview, emit-once or persistence behavior. Existing hybrid/readiness tests cover
matching, one-sided and missing-intent recovery and divergent refusal instruction clauses.
Strict stale/worktree recovery refusal is not an executable native mechanism in these surfaces;
actual host behavior remains the Pi portion of 3.1 and later managed proofs, as parent confirmed.

Native runtime harness: N/A for this characterization-only unit; focused Go admission execution
and asset reads are the actual boundaries. No native state machine or additional class was invented.
Rollback: remove only the new test functions and this unit's progress sections using `baseline/`;
preserve original test bytes, all unit 1 source/assets, task checkboxes and native history/budgets.
`unit1-byte-comparison.json` compares 2,299 files with the prior verifier's post-hashes: only the
three authorized unit 2 surfaces differ. Baseline copies and `baseline.json` preserve exact bytes.
Final incremental A+D: 142 (32 admission tests + 42 instruction tests + 68 progress; no deletions),
within the admitted 400. See `counts.json`, `task-readback.json`, `diff-check.*` and `process-readback.json`.
Tasks are byte-identical (4 checked/10 unchecked). All owned commands terminated; no rollback executed.

### Key Learnings — unit 2

Admission grants are executable Go policy; managed instruction assertions cannot prove host
execution. Keep those evidence layers separate and leave cross-repository task completion pending.

## Quoted diagnostic assertion — independent verification pending

The sole stale CLI expectation now matches the emitted quoted change name.
Its no-marker, no-consent and preparation checks remain; production is unchanged.
The real spaced-name roundtrip remains intact. The prior independent failure is
preserved at `/tmp/sdd4484-ai1-quote-verify.JQDdiS/unit-report.md`.
Fresh independent verification and failure-bound settlement are still required.

## F1 correction — independent verification pending

The quoting correction and a real emitted-command roundtrip test are present:
27 authored A+D across production and organic tests, measured against the prior
verifier's snapshot. Writer `mtx41w6d-8-di43` timed out without a final report;
full-suite completion is not inferred from that run. Logs remain at
`/tmp/sdd4484-f1-_2a4fkeo/`. Parent observed no Go/compiler/test process with a
working directory inside this worktree. Independent verification and failure-bound
settlement remain pending; no next unit or delivery is accepted. This section
supersedes older current-state claims below without erasing their evidence.

## Current correction and acceptance state

Only task 1.1 is accepted and checked. Tasks 1.2, 1.3, and 1.4 remain unchecked;
1.2/1.3 were reopened for independent proof gaps, not because all partial implementation
was discarded. The prior attempt is closed FAILED (parent-provided settlement); independent
verification established a 401 authored A+D floor and refuted the writer's 400 claim.
No new application tests ran for this planning correction and no implementation GREEN
or successful acceptance is claimed. Parent owns reconciliation and the required audited
reset before another execution launch. Policy changes do not reset finite budgets,
rewrite history, grant consent, or authorize a retry by themselves.

Current policy retains seven internal units (AI 3, Pi 4), each strict-TDD implementation
then independent verification before advancement. The 400 authored A+D figure is a target,
with justified cohesive overruns permitted. Exactly one final PR per repository is intended,
both `size:exception`; delivery strategy `exception-ok`, supported chain value
`size-exception` meaning no PR chain. No child branches, PRs, commits, labels, merge,
release, or immediate delivery are authorized. The historical claims below are preserved
as evidence provenance, not current policy or accepted completion.

## Historical writer record — superseded claims retained

Status consumed: `applyState=ready`, `nextRecommended=apply`, repo-local allowed root; no action-context warning.
Completed/task checkboxes: 1.1 preservation, 1.2 RED, 1.3 GREEN are `[x]`.
Files: `internal/cli/{sdd_status.go,sdd_attempt.go,sdd_*marker*_test.go,sdd_status_continue_consent_test.go,sdd_attempt_test.go}`, `internal/sddstatus/{status.go,edit_authority_consent.go,status_v2_clean_break_test.go,edit_authority_test.go,edit_authority_exits_test.go,runtime_topology_guard_test.go}`, and three paired status/continue assets.
## TDD Cycle Evidence
| Cycle | Command, exit, evidence |
|---|---|
| baseline / 1.1 GREEN / 1.2 RED / 1.3 GREEN | `go test ./internal/sddstatus -count=1 -json` 0 `/tmp/sdd4484-ai-status.igT3gN/{preservation-baseline,1.1-preservation-green}.jsonl`; `go test ./internal/cli ./internal/sddstatus -count=1 -json` 1 `/tmp/sdd4484-ai-status.igT3gN/1.2-red.jsonl` (named status/stale-marker failures), then 0 `/tmp/sdd4484-ai-status.igT3gN/1.3-green-retry.jsonl` |
Deviation/risk: no intentional deviation; task 1.4 cannot be added without exceeding the 400-line child cap.
- [ ] **Start → end:** Start with passing primary status/continue tests; add malformed marker, publication failure, valid grant replay, Engram-only no-marker, and nullable discovery triangulation; end with shared internal missing-root facts rather than diagnostic parsing and no new public field/token/reason enum.
PR boundary: AI PR 1 `ai-status-consent`; authored delta is 400 including task/progress; rollback changes only this unit's production/tests/assets, never markers/history/budgets; no commit/delivery.

## Independent evidence and remaining work

- [Independent failed-attempt report](/tmp/sdd4484-ai1-verify.RW6sD2/verification-report.md):
  focused uncached package tests passed, full `go test ./... -count=1` failed, and
  `go vet ./...` passed in that verification run. These are historical report results,
  not commands rerun by this planning writer. The evidence bundle is unchanged.
- The full suite had exactly two reported candidate-caused fixture failures:
  `TestSDDEditAuthorityConsentGrantLoop` and `TestSDDSameParentRepositoryConsentGrantLoop`
  in `e2e/organicruntime/sdd_consent_wiring_e2e_test.go`. Both migrations belong in AI
  unit 1: explicit built-binary continue, then read-only status re-entry, retaining all
  original assertions and a read-only `consentStatus` helper. Full-suite independent
  acceptance is still required; fixture repair alone does not complete the missing proofs.
- Historical genuine RED is limited to `TestSDDStatusDoesNotPrepareConsentMarker` and
  `TestSDDAttemptRefusesRecreatedMarker`. The proposed planning-marker and marker-scope
  refusal cases already passed before implementation and are preservation coverage.
  The verifier inspected historical logs, not an immutable pre-change source replay.
- Pending native proofs include repeated absent/present marker/artifact/authority/root
  snapshots, explicit planning preparation versus ungranted source authority, concurrent
  winner readback, malformed/publication/readback failures, selection/containment and
  unwritable-at-entry refusal, and detected replacement yielding no usable consent.
  Stale A invocation must also be tested against prepared distinct B with A's grant retained;
  reuse existing valid replay preservation. Engram-only and nullable discovery permutations
  remain pending. A directory at the marker filename proves filesystem failure, not permission.
- True read-only/excluded-marker human scope is host-owned in Pi tasks 2.2–2.3/5.1.
  Invalid entry selection/containment/unwritable directory must publish nothing; concurrent
  replacement detection requires no usable consent, not universal zero writes across a
  pathname race. Shared publisher fallback partial visibility remains a limitation/proof
  gap, not authorization to redesign it or claim universal atomic visibility.

## Forecast correction and next gate

[Local conditional estimate](/tmp/sdd4484-ai1-estimate.c2x_ee6_/estimate.md) recorded
711–881 authored A+D from the original implementation baseline and 350–560 next-edit A+D,
both BEFORE this three-file planning correction. Its proposed 950 ceiling was conditional,
not ratified; it installs no new hard gate or native limit. The previous 25–40 documentation
allowance does not absorb this correction: report its actual before/after A+D separately.
Cohesive behavior, both fixture migrations, and missing proofs justify the larger unit,
not compression, removed assertions, or deferring broken behavior to a later unit.
Other unit estimates remain provisional. Each eventual final PR must count full planning
documents wherever included, not present an implementation-only subtotal as its total.

Both links above are local TMP artifacts, available at correction time but not durable
repository evidence. Historical `/tmp/sdd4484-ai-status.igT3gN/` references remain as recorded;
the independent report describes retained copies. Parent must preserve durable evidence
through the existing authorized process before relying on these links beyond this host.
The frozen bundle was not modified. No native attempt operation, reset, review, index,
source/test/asset edit, install, network, or delivery operation occurred in this correction.
Only planning changed; the parent owns the next launch and its admission/reconciliation.

## Planning-only measurement

| File | Additions | Deletions |
|---|---:|---:|
| `tasks.md` | 52 | 52 |
| `design.md` | 37 | 6 |
| `apply-progress.md` | 81 | 0 |

Total: 228 authored A+D against the three files immediately before this correction.

## Fresh AI unit 1 completion — independent acceptance pending

The fresh provider status at `/tmp/sdd4484-ai-status-completion.sERfHu.json` was consumed:
apply ready, one completed task, no blockers, repo-local selected workspace/edit root. Parent admission
superseded the historical budget stop. No attempt operation or delivery was performed by this writer.
Tasks 1.2–1.4 are now implemented and checked after focused and full-module GREEN; independent
verification still belongs to the parent before another internal unit. This is not final SDD verification.
The correction retains failed evidence binding `sha256:c2085af3d8c1282f0e278d918706c4786880ebf63c8f58b1364a08b40e0ec1fe`;
only the parent's fresh independent evidence and settlement can discharge that obligation.

### TDD Cycle Evidence — fresh completion

All logs below are complete files under `/tmp/sdd4484-completion-5n83wyph/`; companion `.exit` files
record exit codes. `baseline/` and `baseline.json` retain exact pre-edit candidate bytes/hashes.

| Cycle | Command | Exit / evidence |
|---|---|---|
| Existing fixture RED | `go test ./e2e/organicruntime -run 'TestSDD(EditAuthorityConsentGrantLoop|SameParentRepositoryConsentGrantLoop)$' -count=1` | 1, `organic-red.txt` |
| Defect RED | `go test ./internal/sddstatus -run TestConsentPreparationRefusesMalformedOrEscapedMarker -count=1 -json` | 1, `marker-red-2.jsonl`: empty status marker and escaped planning home |
| Publication RED | `go test ./internal/sddstatus -run 'TestConsent(PreparationConcurrent|Publication)' -count=1 -json` | 1, `publication-red.jsonl`: uncertain publication error accepted; prior `publication-seam-red.jsonl` is only missing-seam compilation evidence |
| Advisory RED | `go test ./internal/sddstatus -run TestStatusReviewLookupFailureIsAdvisory -count=1 -json` | 1, `advisory-red.jsonl` |
| GREEN / triangulation / refactor | `go test ./internal/cli ./internal/sddstatus -count=1 -json` | 0, `focused-final.jsonl`; named run/pass records retained in `named-test-records.json` |
| Built-binary GREEN | `go test ./e2e/organicruntime -run 'TestSDD(EditAuthorityConsentGrantLoop|SameParentRepositoryConsentGrantLoop|SingleRepoStatusStaysByteIdenticalWithZeroConsentFootprint)$' -count=1` | 0, `organic-green.txt`; status helper remains read-only |
| Asset GREEN | `go test ./internal/assets -count=1 -json` | 0, `assets-restored.jsonl` |
| Full suite | `go test ./... -count=1` | 0, `full-final.txt` and final candidate `full-restored.txt`; `full-first.txt` failed on candidate-caused RAR status and two obsolete asset pins, all explicitly approved and migrated |
| Quality | `go vet ./...`; `go run ./internal/gofmtcheck`; `git diff --check` | 0, `vet-final.txt`, `gofmt-check.txt`, `diff-check-final.txt` |

Coverage-gap characterization includes eight concurrent preparers, repeated CLI/resolver absent/present
marker/artifact/authority snapshots, ungranted source roots, retained A grant vs absent and prepared B,
filesystem permissions, publication/readback failures, detected replacement, valid replay reuse,
Engram topic locators and nullable discovery. Escaped change symlinks are excluded from active selection:
the initial error-only assertion was corrected to accept a blocked/no-selection/no-publication response.
The initial Engram-null-root assertion was corrected to its existing topic locator, without production changes.
`assets-final.jsonl` and `full-candidate.txt` also caught an accidentally removed frozen-projection wording pin; the documentation wording was restored, preserving the assertion.
These fixture corrections are not defect RED. Human excluded-marker/read-only authorization remains host-owned;
copy-fallback atomic visibility is not claimed. No shared publisher, ledger, CAS, or accounting rewrite.

Rollback: revert this unit's native status/continue/marker changes, paired assets and their test migrations
(including both built-binary loops) together; never remove persisted markers, history, or budgets.
The 400-line target overrun is justified by cohesive fixture migrations and missing safety proofs, not
weakened assertions or deferred repair. Exact incremental and full-scope measurements are in `counts.json`.
Fresh incremental A+D: 597/650 including tasks/progress; original-base code/tests/assets: 890.
Current full eventual-PR footprint: 2,752 A+D including complete untracked planning documents (not final delivery scope).
Writer runtime: `PI_PROVIDER=openai-codex`, `PI_MODEL=gpt-6-astra`; effort is not exposed in inspected environment.

### Remaining tasks (unchanged; not part of AI unit 1)
- [ ] **Start → end:** Start from accepted current supported-action behavior; characterize supported status rendering and refusal-before-execution without preserving the exact-three defect; end with a baseline that remains valid after v2 expansion.
- [ ] **Start → end:** Start with the baseline; end with failing cases for all emitted action tokens, seven dependencies, optional four instruction groups, nullable discovery, wrong identity, malformed/unknown action, misleading prose, and separate status/continue calls.
- [ ] **Start → end:** Start from 2.2 RED; end with native v2 decode/rendering and `sddStatus` read-only, a narrow authorized `sddContinue` adapter call, and no live local readiness reconstruction, `resolve-via-engram`, prefixed-token inference, automatic fallback, or `instructions` alias.
- [ ] **Start → end:** Start with closed native declaration/admission and the currently supported managed route; end with separate preservation cases for exact class grants, matching/one-sided/missing-intent recovery, strict stale/divergent refusal, confirmed proposal handoff that does not interview, and automatic unresolved choices emitted once as a lossless grouped prompt—without encoding a whole-phase tool denial as desired behavior.
- [ ] **Start → end:** Start with failing cases for documentation/open-web exact selected grants, inactive/missing extension tools, separately authorized read/write/Engram persistence, narrowed OpenSpec path, wrong worktree, and denial persistence; also require missing-tool denial → corrected capability/artifact facts → continuation with the **same** bounded selected-store path/scope, never a broadened or replacement scope. End with existing launch data carrying selected classes/per-class grants and actual extension selection, with child-local inventory recheck.
- [ ] **Start → end:** Start from existing runtime tests; end with distinct characterization cases for finite failed/interrupted/no-diff settlement, replay receipt idempotency, caps, failed-evidence binding, and fresh-selection strictness, without asserting the reported successor-acquire connection already passes.
- [ ] **Start → end:** Start with RED cases from the original failed baseline for successor-acquire replay, later-drift strictness, lost reply, and uncertain pre-settlement effect; end with the smallest proven connection through existing predicates, or a documented no-source-change result if the RED premise is invalidated by current behavior.
- [ ] **Start → end:** Start with RED tests requiring `remediate` to refuse if unsupported, carry `failedEvidenceRevision` unchanged when supported, acquire once, and settle pass/fail/interruption through existing compact JSON; end with narrow adapter methods and runner finalization using retained session/task history, not a second ledger.
- [ ] **Start → end:** Start with a failing controlled boundary case using the built AI binary after its internal units pass independent verification, fixed Pi extension selection, and installed assets; end with a managed child that receives native v2 action/context, selected research/persistence tools, and typed remediation support, or fails safely before work when an asset/provisioning capability is deliberately absent.
- [ ] **Start → end:** Start after all internal work units have independent acceptance and their RED/GREEN/TRIANGULATE/REFACTOR records and the 5.1 tuple passes; end with independently recorded full-suite results, diff/asset/record inspection, spec traceability, and a maintainer review package—never a delivery action.

## AI unit 3 preflight — admission ownership clarification pending

Consumed fresh `/tmp/sdd4484-ai-status-unit3.oVfLDE.json`: apply ready, 4 checked/10 pending,
no blockers, repo-local authorized workspace; no action-context warning. Read proposal, all three
specs, design, tasks, progress and strict-TDD config; loaded all five injected project/user skills.
Parent reports units 1 and 2 independently PASS and their native objectives COMPLETE. Unit 2's
independent evidence is `/tmp/sdd4484-ai2-verify.YHYL8c/`, manifest
`c31eee4c257edf37f41d15a3f5061b33763ab8e73457e07cdfd6416b35a98ce8`, 142 A+D.
These are parent-provided acceptance facts, not independently rerun here. Narrow the older unit 2
blanket PID claim: diff-check recorded command/output/exit, not PID. Whole 3.1 remains unchecked.

Execution paused on conflicting admission instructions: injected native instructions mandate an
executor acquire before runtime-bearing work; this launch explicitly forbids child acquire and
retains the token with the parent. Parent clarification query timed out. No acquire, settle, reset,
runtime test, production/test edit, actor launch or delivery operation was performed.
CodeGraph read-only mapping reached Acquire → runtimeBeginAdmission → historical selection helper;
this is structural evidence only, not successor CLI, replay, uncertainty or accounting execution proof.

TDD Cycle Evidence: preservation / RED / GREEN / triangulation / refactor all NOT RUN.
Tasks 4.1 and 4.2 remain unchecked; no defect or no-defect disposition is established.
All ten exact unchecked task lines remain in the preceding Remaining tasks section and tasks.md.
Baseline bytes/hashes: `/tmp/sdd4484-ai3-preflight-y5buvd_7/baseline/` and `baseline.json`.
Only this progress section changed; rollback removes this section, preserving all earlier units,
task checkboxes and native authority. No owned process remains. Delivery remains one eventual
size-exception PR per repository, no chain; this preflight is not a PR boundary or final verification.

## AI unit 3 retry — preservation interrupted, tasks still pending

Fresh retry status: apply ready, 4 checked/10 pending, repo-local authorized root, no blockers.
The authorized same-attempt token continuation returned `proceed`; no new attempt was opened.
Evidence: `/tmp/sdd4484-ai3-retry-91nz2_ug/` contains exact 2,299-file baseline bytes/hashes.
TDD Cycle Evidence: `go test ./internal/sddstatus -count=1 -json` passed 750/750, exit 0;
`go test ./internal/cli -count=1 -json` was terminated by the tool's 60-second timeout after
955 run/948 pass/0 fail records; no terminal package result or exit code was captured.
This is an infrastructure interruption, not defect RED or a no-defect disposition. No retry,
production/test edit, refactor, full-suite run or completion claim occurred; 4.1/4.2 stay unchecked.
Strict-TDD safety-net stop applies. Process readback found no matching Go/test/compiler process.
Parent owns reconciliation/settlement and independent acceptance; no final SDD verification.
Rollback removes only this retry section; earlier units, task bytes and native history are preserved.
Boundary: AI internal unit 3, one eventual size-exception AI PR, no chain or delivery action.
Writer: `openai-codex/gpt-6-astra`; effort not exposed. Project skills paths-injected;
phase skill resolved through fallback path `/home/gentleman/.agents/skills/sdd-apply/SKILL.md`.

## AI unit 3 — current bounded continuation

Injected native status consumed: apply ready, repo-local authorized root, no blockers/warnings.
Authorized existing-attempt continuation returned proceed; parent owns settlement and acceptance.
Evidence: `/tmp/sdd4484-ai3-current-p7ro6kv7/`; 2,299 baseline files copied mechanically.
Task 4.1 is checked using existing distinct preservation cases, without duplicate tests or production edits.

### TDD Cycle Evidence
| Task/stage | Evidence and disposition |
|---|---|
| 4.1 preservation | `baseline-status.*`: 750 run/pass, exit 0; `baseline-cli.*`: 2,709 run, 2,708 pass, one skip, package exit 0 |
| 4.1 RED/GREEN/triangulation/refactor | Preservation only, not defect RED; existing outcome, cap, replay and freshness permutations reused; no refactor |

Named records are retained in `baseline-named-records.json`. Reused coverage includes
`TestCompactSettlePreservesFailedEvidenceAndReplay`, both `TestRuntimeFinishRecordsTruthful*`,
`TestInterruptedCallThatDeliveredNothingStillSpendsTheBudget`, `TestRefundsAreCappedAtTheConfiguredAttemptCeiling`,
`TestFailedInvalidatedZeroLineSettlementRefundsTheAttempt`, `TestFailedSettlementKeepsChargedWhenNotBothInvalidatedAndZeroLine`,
`TestRuntimeFreshBeginStillRefusesTrackedIntendedUntracked` and `TestDeriveRemediationEvidenceRevision`.
These exercise native ledger/CLI fixtures, not Pi or arbitrary external-effect execution.
4.2 remains pending; all other unchecked lines remain exactly in tasks.md and the historical Remaining tasks section.

### AI unit 3 completion — parent independent acceptance pending

Tasks 4.1 and 4.2 are implemented and checked; whole 3.1 stays unchecked for Pi. This supersedes the pending 4.2 note above, not prior interruption history.
Parent explicitly approved `internal/sddstatus/runtime_admission.go` after the real CLI RED. No further admission, reset, settlement, actor or delivery was performed.
Production changes only the inherited successor-selection connection in `runtime_admission.go`, `runtime_compact.go`, and `runtime_ledger.go`; explicit fresh selection remains untouched.
Existing `runtimeReplayedIntendedUntracked` reconciles inherited paths; errors propagate, terminal decisions stay ahead of capture, and all three callers use the same helper.

### TDD Cycle Evidence / Work Unit Evidence
| Task/stage | Command, actual terminal result and evidence |
|---|---|
| 4.2 RED | `go test ./internal/cli -count=1 -json`: exit 1, sole failed test `TestRuntimeSuccessorAcquireReconcilesHistoricalUntracked`; `successor-red.*`, frozen `successor-red-test.go` |
| RED confirmation while awaiting scope approval | `go test ./... -count=1`: exit 1, same successor failure; `blocked-full.*`; not an infrastructure interruption |
| 4.2 GREEN | Identical original test, full CLI command: exit 0, 2,710 run / 2,709 pass / one existing skip; `successor-green.*`, `green-candidate/` |
| TRIANGULATE / test-fixture refactor | Full CLI command: exit 0, 2,719 run / 2,718 pass / one existing skip; `triangulate-cli.*`, `triangulate-named-records.json` |
| Preservation after fix | `go test ./internal/sddstatus -count=1 -json`: exit 0, 750 run/pass; `final-status.*` |
| Full module / quality | `go test ./... -count=1`, `go vet ./...`, `go run ./internal/gofmtcheck`, `git diff --check`: all exit 0; `final-full.*`, `final-vet.*`, `final-format.*`, `final-diff.*` |
| Runtime boundary | Actual `RunSDDAttempt` CLI/preflight/ledger in isolated Git fixtures; no managed child or remote effect execution |
| Rollback | Restore only this unit's three production-file hunks from `baseline/`, remove new `internal/cli/sdd_attempt_recovery_boundary_test.go`, reopen 4.1/4.2 and remove this continuation's appended progress; preserve all prior units, history, authority and budgets |

Successor triangulation proves read-only status plus acquire/begin across staged, commit and commit-a landings: four records, two lifetime/charged attempts, zero lifetime lines, unchanged prior record bytes.
Fresh caller-selected tracked paths refuse before mutation; later content drift still blocks status/acquire. Shared caller execution is proven; synthetic helper-error injection was not added, so error propagation is code-inspection evidence.
Lost committed reply uses a failing output writer after settlement and exact same token/ID/payload replay; two records, one charged/lifetime attempt, zero lines, retained failed evidence and conflicting-payload refusal.
The evidence revision is independently recomputed from actual read-only Git invocation/artifact bytes. Raw fixture records and invocation bytes are extracted in `observed-fixtures/` with `observed-fixture-records.json` (fixture tokens only, never the real active token).
Unknown-effect coverage records an unavailable acknowledgement, refuses missing cleanup facts and a competing acquire, then settles interrupted with uncertainty intact; no successful external operation, arbitrary exactly-once execution or automatic retry safety is claimed.
Lost-reply/uncertainty cases passed first run: coverage-gap preservation, not additional defect RED. No production settlement, evidence, refund or verification policy changed; no further production refactor was needed.
The one unchanged CLI skip is `TestNegotiatedRecoverTransitionRunsVerbatimForStagedPredecessorAfterRebase`: its fixture routed to start/fresh_target_ready, not recover; that skipped leg is not execution evidence.
`prior-baseline-comparison.json` confirms all 2,299 entry files match the previous retry snapshot except prior progress. `counts.json`, `candidate-comparison.json`, `task-readback.json` and `process-readback.json` record final delta, preservation, checkboxes and owned-process termination.
Workload: AI internal unit 3 only, one eventual size-exception AI PR, no child chain or delivery; native 357-new-line allowance retained, with 43 prior charged lines. Exact measured final totals are in `counts.json`.
Writer: `openai-codex/gpt-6-astra`; effort not exposed. Project skills paths-injected; phase skill fallback-path. Parent independently verifies and owns settlement; no final SDD/Pi acceptance or verify-report is claimed.

### Remaining tasks — exact persisted unchecked lines
- [ ] **Start → end:** Start from accepted current supported-action behavior; characterize supported status rendering and refusal-before-execution without preserving the exact-three defect; end with a baseline that remains valid after v2 expansion.
- [ ] **Start → end:** Start with the baseline; end with failing cases for all emitted action tokens, seven dependencies, optional four instruction groups, nullable discovery, wrong identity, malformed/unknown action, misleading prose, and separate status/continue calls.
- [ ] **Start → end:** Start from 2.2 RED; end with native v2 decode/rendering and `sddStatus` read-only, a narrow authorized `sddContinue` adapter call, and no live local readiness reconstruction, `resolve-via-engram`, prefixed-token inference, automatic fallback, or `instructions` alias.
- [ ] **Start → end:** Start with closed native declaration/admission and the currently supported managed route; end with separate preservation cases for exact class grants, matching/one-sided/missing-intent recovery, strict stale/divergent refusal, confirmed proposal handoff that does not interview, and automatic unresolved choices emitted once as a lossless grouped prompt—without encoding a whole-phase tool denial as desired behavior.
- [ ] **Start → end:** Start with failing cases for documentation/open-web exact selected grants, inactive/missing extension tools, separately authorized read/write/Engram persistence, narrowed OpenSpec path, wrong worktree, and denial persistence; also require missing-tool denial → corrected capability/artifact facts → continuation with the **same** bounded selected-store path/scope, never a broadened or replacement scope. End with existing launch data carrying selected classes/per-class grants and actual extension selection, with child-local inventory recheck.
- [ ] **Start → end:** Start with RED tests requiring `remediate` to refuse if unsupported, carry `failedEvidenceRevision` unchanged when supported, acquire once, and settle pass/fail/interruption through existing compact JSON; end with narrow adapter methods and runner finalization using retained session/task history, not a second ledger.
- [ ] **Start → end:** Start with a failing controlled boundary case using the built AI binary after its internal units pass independent verification, fixed Pi extension selection, and installed assets; end with a managed child that receives native v2 action/context, selected research/persistence tools, and typed remediation support, or fails safely before work when an asset/provisioning capability is deliberately absent.
- [ ] **Start → end:** Start after all internal work units have independent acceptance and their RED/GREEN/TRIANGULATE/REFACTOR records and the 5.1 tuple passes; end with independently recorded full-suite results, diff/asset/record inspection, spec traceability, and a maintainer review package—never a delivery action.

## j96 correction self-checks — independent acceptance pending
Fresh status: apply ready, repo-local authorized root, 6 checked/8 pending; current-token continuation proceeded, with no scope warning.
Evidence: `/tmp/sdd4484-j96-final.fqmnbfc_/unit-report.md`; all 20 fresh command gates exited 0 with pinned process-local offline Go caches.
| TDD Cycle Evidence: RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|
| Frozen supplemental `a532823b950788c6143d8f9c44814942cf3194b8707293685fc945285b2f7998`: stale j96 pin, not a new production defect | Root/bench uncached suites, builds, vet, format, diff and five exact CI jq assertions passed | Portable 69, transition 80, focused j96 and j105 completed; tagged j97 completed, untagged j97 unsupported as required | None; corrected source preserved byte-for-byte |
All 2,300 entry files were unchanged before this 10-line append; tasks remain 6 checked/8 pending, with exact unchecked lines above. Prior partial evidence is not full GREEN.
Boundary: unit1 fixture follow-up under ai-runtime-recovery; 362 prior + 10 new A+D = 372/400; two eventual size-exception PRs, no chain or delivery. Rollback removes only this append.
Parent independent corrected-evidence verification and settlement naming the failed revision above remain required; no final SDD/Pi acceptance or settlement is claimed.

## R3 bounded correction — blocked before implementation
Fresh native v2 status: apply ready, repo-local authorized root, no blockers/warnings, 6 checked/8 pending.
Same admitted attempt continuation returned proceed; parent retains review, verification and settlement ownership.
Stopped at the explicit minimal-correction gate: no source/test edits, no task completion, no contract weakening.
Source: CLI grant checks at `sdd_attempt.go:265`; `Grant` calls the clock then mutate at `runtime_ledger.go:1802–1803`.
Mutation receipt replay precedes build (`2182–2195`); record publication and HEAD commit are separate (`2241–2268`).
Any last marker reread can accept A before an uncooperative replacement installs B and the A record commits.
A post-commit refusal cannot undo immutable authority append; the ledger lock does not coordinate marker replacement.
Distinct-instance filtering at `2974–2977` still prevents B from projecting A roots; stale append is not root inheritance.
Parent decision needed: bounded detection at mutation/receipt checks (forecast 160 A+D: 35 source, 105 tests, 20 progress),
or retain universal no-stale-append and separately design ownership/synchronization; no defensible <=200 estimate for that scope.
Neither option was implemented or authorized by this note; the 200 hard cap and existing assertions remain unchanged.
| TDD Cycle Evidence | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| R3 | Not run: feasibility stop | Not written/run | Not run | Not run | None |
All 20 verification gates NOT RUN; no CLI or native regression execution is claimed. No owned test process launched.
Private evidence: `/tmp/sdd4484-r3-fix.38vljjfi/`; source/test 0 A+D, progress 21 A+D; tasks byte-identical.
All eight exact unchecked task lines remain in the preceding remaining-task section and persisted tasks.md.
Boundary: existing AI correction only, eventual size-exception PR per repository, no chain/Pi/delivery or native review action.
Rollback removes only this section; parent correction validation and settlement remain pending.

## R3 current-instance binding — bounded correction, parent acceptance pending
Human selected bounded detection: external replacement may leave immutable historical grants; distinct instances inherit no roots.
Fresh native v2 apply ready, repo-local authorized root/no warnings; same-token continuation proceeded. Tasks remain 6 checked/8 pending.
CLI now uses opt-in ForCurrentChangeInstance; grant-only mutation checks precede receipt replay and check successful return under the existing lock.
Detected stale success returns no authority and identifies the committed receipt without unsafe replay guidance; existing publication errors are preserved.
| TDD Cycle Evidence | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|
| Native grant boundary | Four real stale-result failures; initial empty-status fixture corrected before final RED | Same assertions/schedules pass; only binding setup migrated with parent approval | Later HEAD retains original receipt identity; valid replay, preserved publication failure and B isolation pass | None needed; gofmt applied |
Preservation: prior real CLI recreated-marker run/pass/exit0 rechecked against identical source; concurrent race proof is internal API, not CLI execution.
Evidence `/tmp/sdd4484-r3-binding.rzn3nwmu/` records exact focused and final20 command outcomes, stage hashes, counts and process cleanup; no independent acceptance inferred.
Budget: prior21 + current166 source/tests +13 progress =200/200 A+D. All eight exact unchecked task lines above remain unchanged in tasks.md.
Rollback removes only this continuation's source/test/progress hunks, never history; parent owns independent verification/native validation/settlement. Two eventual size-exception PRs, no chain/Pi/delivery.
