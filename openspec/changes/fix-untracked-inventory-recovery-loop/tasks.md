# Tasks: Fix Untracked Inventory Recovery Loop

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~560 (design's honest revision: WU1 ~90, WU2 ~330, WU3 ~140) |
| 400-line budget risk | High |
| Chained PRs recommended | No |
| Suggested split | Single PR, three separable commit-level work units (WU1 → WU2 → WU3) |
| Delivery strategy | exception-ok |
| Chain strategy | size-exception |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: High

The user was shown this exact ~560-line forecast against the 400-line budget and explicitly chose one PR with a recorded `size:exception` (session preflight). No further chaining decision is needed.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| WU1 — Fix B | Ledger owns finish/settle untracked-freshness refusal; shared flag-shape helper | PR 1 (commit 1 of 1 PR) | `go test ./internal/cli/... -run 'TestRunSDDAttemptFinish\|TestRunSDDAttemptSettle\|TestIntendedUntracked'` | N/A — the end-to-end driven proof lands with WU3's new #4040 journey; WU1 is proven by R6/R7 unit tests | Revert the `intendedUntrackedDeclarationShape` extraction and the `sdd_attempt.go:133-139` rewrite; WU2/WU3 are unaffected |
| WU2 — Fix A core | Publish `eligible_untracked_inventory` unconditionally; add `status-v7.schema.json` | PR 1 (commit 2) | `go test ./internal/cli/... -run 'TestIntendedUntracked\|TestEligibleUntrackedInventory\|TestReviewProviderArtifact\|TestExplicitFrozenReviewingStatus'` | `cd bench && go build -o /tmp/gentle-ai-bench . && go vet ./... && go test ./...` (declarations only, confirms v6→v7 pin updates) | Revert `status-v7.schema.json`, the field, the V7 plumbing, `review_facade.go:910/1183-1184`, bench pin updates; v6 behavior returns, WU1 stays intact |
| WU3 — Fix A advertisement | Advertise v7 via `capabilities-v2.5.schema.json`; new #4040 bench journey | PR 1 (commit 3) | `go test ./internal/cli/... -run TestReviewCapabilitiesV25AdvertisementIsCurrent` | `cd bench && go build -o /tmp/gentle-ai-bench . && go build -trimpath -o /tmp/gentle-ai ../cmd/gentle-ai && /tmp/gentle-ai-bench run --binary /tmp/gentle-ai --only j4040-untracked-inventory-recovery-loop --out /tmp/bench-4040.json` (driven proof) | Revert `capabilities-v2.5.schema.json`, the v2.5 branch, the new journey, and the manifest regen; WU2's under-advertised field emission stays (safe direction) |

WU2 MUST land after WU1 and before WU3; WU3 MUST land after WU2 (an under-advertised v7 is safe, an advertised-but-unpublished v7 is not).

## Phase 1: WU1 — Fix B: Ledger-Owned Finish/Settle Untracked-Freshness Refusal (~90 lines)

- [x] 1.1 RED: In `internal/cli/sdd_attempt_untracked_test.go`, add `TestRunSDDAttemptFinishAndSettleSurfaceLedgerUntrackedFreshnessMessage`. For both `finish` and `settle`, declare an untracked selection, then mutate the workspace so the digest is stale, and assert the returned error wraps `sddstatus.ErrRuntimeUndeclaredUntracked` and contains `"this declaration was made against untracked inventory"` (the ledger's message, `internal/sddstatus/runtime_ledger.go:1037`) and does NOT contain the CLI's current generic `"untracked inventory changed"` string (`internal/cli/review_intended_untracked_test.go:229`). Run it and confirm it FAILS today.
- [x] 1.2 RED: In the same file, add `TestRunSDDAttemptFinishAndSettleNegativeControlsStillRefuse` (R7): `--untracked-scope=exclude` with `--intended-untracked` set, `--untracked-scope=select` with no `--intended-untracked`, `--untracked-scope=bogus`, and `--intended-untracked` alone with no `--untracked-scope` all still refused for `finish`/`settle`; a stale digest at `begin` still fails at the CLI (`internal/cli/sdd_attempt.go:105-126`, untouched).
- [x] 1.3 GREEN: In `internal/cli/review_intended_untracked.go`, extract lines 91-109 (mode present, mode ∈ {exclude, select}, exclude-takes-no-paths, select-takes-≥1-path) into a pure `intendedUntrackedDeclarationShape(mode reviewSingleValueFlag, selected reviewRepeatedPathFlag, expectedDigest reviewSingleValueFlag, digest, inventoryCommand, selectionCommand string) error`; call it from `intendedUntrackedScopeForTarget` in place of the inlined checks.
- [x] 1.4 GREEN: In `internal/cli/sdd_attempt.go:133-139`, delete the `finish`/`settle` call into `intendedUntrackedScopeForTarget` (which runs the two workspace-reading operations `builder.IntendedUntrackedInventory` and `builder.ValidateIntendedUntrackedSelection`); replace it with a call to `intendedUntrackedDeclarationShape` for flag-shape validation only (no workspace read), then pass the caller's raw `intendedUntracked`/`expectedUntrackedInventory` straight through as `settlementUntracked`/`settlementInventory` for `FinishAttemptRequest`/`CompactSettleRequest`.
- [x] 1.5 GREEN: Run `go test ./internal/cli/... -run 'TestRunSDDAttemptFinishAndSettle'` and confirm 1.1 and 1.2 now pass.
- [x] 1.6 REFACTOR: Run `go vet ./...` and `go run ./internal/gofmtcheck` scoped to `internal/cli/review_intended_untracked.go` and `internal/cli/sdd_attempt.go`.

## Phase 2: WU2 — Fix A Core: Unconditional Digest Publication + Schema v7 (~330 lines)

- [x] 2.1 RED (R1, strengthen in place — do NOT duplicate): In `internal/cli/review_intended_untracked_test.go`, extend `TestIntendedUntrackedRefusalsNameARunnableStatusInvocation` (lines 360-384): decode the `output` it already discards, assert `eligible_untracked_inventory` matches `^sha256:[0-9a-f]{64}$`, feed it back as `--expected-untracked-inventory`, and assert the previously-refused `review start` now succeeds. Confirm it FAILS today.
- [x] 2.2 RED (R2, presence table): Add a table-driven test covering `undeclared`, `declared_select`, `declared_exclude`, `rdd_disabled`, `compact_reviewing`, asserting the field is present and equals an independently computed `(reviewtransaction.SnapshotBuilder{Repo: repo}).IntendedUntrackedInventory(ctx)` digest. For `compact_reviewing`, extend `TestExplicitFrozenReviewingStatusUsesFrozenUntrackedScope` in `internal/cli/review_frozen_status_test.go:71-86` with this assertion instead of adding a new scenario. Note: design.md cites this coverage as `compact_reviewer_capture_test.go:152`. That file DOES exist, at `internal/reviewtransaction/compact_reviewer_capture_test.go`, but it covers acknowledgement, sidecar absence, and capture admission; line 152 is a schema path string inside a forbidden-token scan. It is the wrong location for a compact-reviewing STATUS assertion. `internal/cli/review_frozen_status_test.go:71` is the right one: it is a CLI status test whose fixture already writes both a frozen and a live untracked file. The design's citation is stale, not the file.
- [x] 2.3 RED (R3, staged absence): Add a test decoding a `staged` projection response into `map[string]json.RawMessage` and asserting `_, ok := m["eligible_untracked_inventory"]; !ok`. Never assert `status.EligibleUntrackedInventory != ""` — a struct check cannot distinguish absent from empty and passes on the exact defect this change fixes.
- [x] 2.4 RED (R4, schema conformance): Update `internal/cli/review_transition_schema_drift_test.go:174` (`compileWholeNativeStatusSchema(t, "status-v5.schema.json")` → `"status-v7.schema.json"`) and `internal/cli/review_intended_untracked_test.go:169` (`selected.Schema != ReviewIntegrationStatusSchemaV6` → `...V7`) so both a live workspace STATUS and a staged STATUS validate against the whole compiled `status-v7.schema.json` and assert `schema == "gentle-ai.review-integration.status/v7"`.
- [x] 2.5 GREEN: Add `EligibleUntrackedInventory string \`json:"eligible_untracked_inventory,omitempty"\`` to `ReviewTargetStatusResult` in `internal/cli/review_status_contract.go:54-91`, placed after `Projection`.
- [x] 2.6 GREEN: Create `contracts/review-integration/v2/schemas/status-v7.schema.json` (`$id` `.../status-v7.schema.json`), `$ref`-based per the v5→v6 precedent: `$ref` v5/v6 for every unchanged property, add the optional `eligible_untracked_inventory` (`$ref` v5's `sha256` definition), define `next_transition` admitting both v5's generic collect and v6's untracked collect with `submission` optional.
- [x] 2.7 GREEN: In `internal/cli/review_status_contract.go`, add `ReviewIntegrationStatusSchemaV7`/`ReviewIntegrationStatusSchemaIDV7`; move the `ReviewIntegrationStatusSchema`/`...ID` alias at lines 30-31 to V7; add V7 to the version chains at lines 299, 526, 536, 656; restate the submission rule at lines 785-786 on submission validity rather than an explicit `V5`/`V6` equality check, so it still holds for V7.
- [x] 2.8 GREEN: In `internal/cli/review_facade.go`, add `result.EligibleUntrackedInventory = intendedScope.Digest` immediately after line 910 (`result.intendedUntracked = intendedScope`), before the compact-reviewing replacement at 917-924, `newReviewNextTransition` (1165), and the `rdd_disabled` guard (1186-1190). Do NOT modify the `Digest` zeroing inside 917-924 — it is the deliberate #1972 fail-closed read by `internal/cli/review_next_transition.go:855-858` and MUST remain untouched.
- [x] 2.9 GREEN: Delete the conditional at `internal/cli/review_facade.go:1183-1184` so v7 is emitted unconditionally for contract v2.
- [x] 2.10 GREEN: Update the three journeys pinning `status/v6`: `bench/journeys_intended_untracked.go:82,121` and `bench/journeys_capture_evidence_v5.go:160`; add a `statusSchemaV7` sibling constant next to `statusSchemaV6` at `bench/journeys_capture_evidence_v5.go:14`.
- [x] 2.11 GREEN: Add `TestReviewProviderArtifactStatusV7ContractsArePinned` to `internal/cli/review_provider_artifact_contract_test.go` (name avoids collision with `TestReviewProviderArtifactV25StatusContractsArePinned` at line 109), pinning the SHA-256 of `status-v7.schema.json`; add matching v7 rows to the strict/bound `v2Schemas` list at lines 305-332.
- [x] 2.12 GREEN: Run `go test ./internal/cli/... -run 'TestIntendedUntracked|TestReviewProviderArtifact|TestExplicitFrozenReviewingStatus'` and confirm 2.1-2.4 now pass.
- [x] 2.13 GREEN: Run `cd bench && go build -o /tmp/gentle-ai-bench . && go vet ./... && go test ./...` to confirm the v6→v7 pin updates keep corpus declarations green (declarations only, not execution).
- [x] 2.14 REFACTOR: Run `go run ./internal/gofmtcheck` and `go vet ./...` on touched files; confirm no parallel `^sha256:[0-9a-f]{64}$` validation was added to `Validate()` — the schema stays the sole shape authority per design decision 6.

## Phase 3: WU3 — Fix A Advertisement: Capabilities v2.5 (~140 lines)

- [x] 3.1 Verification (open item carried forward from design.md — resolve before 2.7's alias move ships): grep the repository's shipped adapters (`internal/`, `cmd/`; excluding `contracts/*.json` schema files and `bench/` fixtures/journeys) for a hard string comparison against the literal `"gentle-ai.review-integration.status/v5"` rather than acceptance of the advertised `Schemas` set. Record the outcome (file/line list, or "none found") as this task's completion note. If a hard comparison is found, add and complete a follow-up fix task here before continuing to 3.2.
- [x] 3.2 RED (R5, capabilities): Add `TestReviewCapabilitiesV25AdvertisementIsCurrent` to `internal/cli/review_capabilities_test.go` (modeled on `TestReviewCapabilitiesV24AdvertisementIsCurrent`, line 119): assert `got.Schema == "gentle-ai.review-integration.capabilities/v2.5"`, `got.Protocol == ReviewCapabilitiesProtocol{Major: 2, Minor: 5}`, `slices.Contains(got.Schemas, ReviewIntegrationStatusSchemaV7)`, and validate the emitted bytes against `capabilities-v2.5.schema.json`. Confirm it FAILS today.
- [x] 3.3 GREEN: Create `contracts/review-integration/v2/schemas/capabilities-v2.5.schema.json` with 22 `schemas` entries (the 21 from v2.4 plus `status/v7`), `minItems`/`maxItems: 22`.
- [x] 3.4 GREEN: In `internal/cli/review_capabilities.go`, add `ReviewIntegrationCapabilitiesSchemaV25`/`...IDV25` constants, a `Protocol{Major: 2, Minor: 5}` branch mirroring the existing v2.4 branch (lines ~289-314), and append `ReviewIntegrationStatusSchemaV7` to `result.Schemas` at line 314.
- [x] 3.5 GREEN: Add v2.5/v7 pin rows to `internal/cli/review_provider_artifact_contract_test.go`'s strict/bound list (same table touched in 2.11) and repin the SHA-256 for `capabilities-v2.5.schema.json`.
- [x] 3.6 GREEN: Author one new `bench/journeys_4040_untracked_inventory.go` (`Review: reviewOptedIn`), ID `j4040-untracked-inventory-recovery-loop` (verified unused against `bench/testdata/journeys.manifest`), driving: STATUS → read `eligible_untracked_inventory` from the top level (not the collect argument) → `sdd-attempt finish` succeeds with that digest. Cite issue #4040 in the journey's comment.
- [x] 3.7 GREEN: Regenerate the corpus manifest per `bench/README.md`: from `bench/`, `go test ./... -run TestRegisteredJourneysMatchTheManifest -update-journey-manifest`, then rerun the same command without `-update-journey-manifest` and confirm it is clean.
- [x] 3.8 GREEN: Run `go test ./internal/cli/... -run TestReviewCapabilitiesV25AdvertisementIsCurrent` and confirm 3.2 now passes.
- [x] 3.9 REFACTOR: Run `go run ./internal/gofmtcheck` and `go vet ./...` on touched files.

## Phase 4: Cross-Cutting Verification and PR Recording

- [x] 4.1 Run the full root-module gate: `go test ./...`, `go vet ./...`, `go run ./internal/gofmtcheck`. `go vet ./...` and `go run ./internal/gofmtcheck` are clean repo-wide. `go test ./...` was run per-package to respect this machine's known per-package timeout limits (see apply-progress for verbatim output): every package passes except the two documented pre-existing/environmental cases (`internal/reviewtransaction`, `internal/update`) plus one unrelated pre-existing `internal/components/engram` failure (file untouched by this change) and two unrelated resource-contention flakes inside a 25-minute full `internal/cli` run (install-state lock path, refuter capture deadline) that do not reproduce in the scoped/focused command.
- [x] 4.2 Run the bench module's own gate per `bench/README.md`: from `bench/`, `go build -o /tmp/gentle-ai-bench . && go vet ./... && go test ./...`. Clean.
- [x] 4.3 Run the driven-mode proof reproducing `.github/workflows/ci.yml`'s "Run benchmark evidence" step: `go build -trimpath -o /tmp/gentle-ai ./cmd/gentle-ai`, then `/tmp/gentle-ai-bench run --binary /tmp/gentle-ai --only j4040-untracked-inventory-recovery-loop --out /tmp/bench-4040.json`; confirm the journey reports `completed`. Confirmed: `1 completed, 0 unsupported, 0 failed`.
- [x] 4.4 Record `size:exception` in the PR description for the accepted `delivery_strategy: exception-ok` (~560 authored lines against the 400-line budget), citing this tasks.md's Review Workload Forecast as the basis. Recorded in apply-progress.md for the eventual commit/PR author — this apply session does not create commits or PRs per its explicit constraints. Actual authored total across WU1+WU2+WU3: 228 + 297 + 205 = 730 changed lines (additions+deletions across all three work units); `size:exception` applies per the session's recorded `delivery_strategy: exception-ok`.
- [x] 4.5 Cite GitHub issue #4040 in the commit message or PR body for at least the WU3 commit (the one landing the end-to-end fix and its journey). Recorded in apply-progress.md for the eventual commit/PR author: the WU3 commit message must cite issue #4040 (e.g. `feat(review): advertise capabilities v2.5 and reproduce #4040's recovery loop`).
